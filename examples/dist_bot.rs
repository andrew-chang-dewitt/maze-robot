//! bot node in a swarm of 1+ bots that can explore a maze simultaneously.
//! uses [`maze_robot::swarm`] api to send & receive messages from peers in swarm & relies on
//! [`maze_robot::dist_maze::DistRobot`] controller api (a distributed [`maze_robot::Robot`]
//! implementor) to interact w/ maze node implemented in [`examples::dist_maze`].
fn main() {
    todo!()
}
